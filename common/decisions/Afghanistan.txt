
Afghanistan_domestic_decisions = {
	#might turn back on
	#might turn back on

	#Import Westernism

	#Join Nato Operations/Wars

	#Join Nato Fairly hard to get

	#Invite Admad Wali Massoud

}

AFG_Northern_alliance_taloqan_siege = {
	AFG_Taloqan_siege_mission = { #taliban dont manage to take the citiy
		fire_only_once = no
		days_re_enable = 5
		activation = {
			410 = {
				has_dynamic_modifier = {
					modifier = AFG_Taliban_assault_on_Taloqan scope = TAL
				}
			}
		}
		allowed = {
			original_tag = AFG
		}
		available = {
			410 = {
				has_dynamic_modifier = {
					modifier = AFG_Taliban_assault_on_Taloqan scope = TAL
				}
			}
			TAL = { controls_province = 12314 }
		}
		icon = FIN_stop_motti_tactics
		days_mission_timeout = 248
		is_good = yes
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision AFG_Taloqan_siege_mission"
			AFG = { country_event = { id = Afghanistan.31 } }
			set_country_flag = AFG_holds_Taloqan
			effect_tooltip = {
				newline = yes
				custom_effect_tooltip = TT_EFFECTS_FROM_EVENT
				add_war_support = 0.15
				custom_effect_tooltip = AFG_enhance_leader_protection_tt
				newline = yes
			}
		}
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision AFG_Taloqan_siege_mission"

			#show_ideas_tooltip = AFG_Taloqan_not_one_step_back
			AFG = { country_event = { id = Afghanistan.32 } }
			#custom_effect_tooltip = AFG_taloqan_text1
			custom_effect_tooltip = TT_EFFECTS_FROM_EVENT
			effect_tooltip = {
				add_war_support = -0.08
			}
			show_ideas_tooltip = AFG_civilian_exodus_Taloqan

		}

	}
}
Afghanistan_pashtunistan_decisions = {











}
AFG_agriculture_mech = {
	# Pomegranate Investment - Limited to 5 uses, modest returns
	# Investment: 5% GDP upfront, Returns: 6% GDP after harvest (20% profit margin)

	# Almond Investment - Limited to 5 uses, modest returns
	# Investment: 5% GDP upfront, Returns: 6% GDP after harvest (20% profit margin)
}

Taliban_insurgency_category = {






	isaf_raid_taliban_hideouts = {
		allowed = { NOT = { original_tag = AFG } }
		icon = GFX_decision_humvee

		custom_cost_trigger = {
			command_power > 40
		}

		custom_cost_text = command_power_more_than_40

		visible = {
			has_country_flag = afg_support_counter_insurgency
			AFG = {
				check_variable = {
					var = taliban_strength
					value = 0
					compare = greater_than
				}
			}
		}

		available = {
			has_military_access_to = AFG
		}

		complete_effect = {
			add_command_power = -40
			log = "[GetDateText]: [This.GetName]: decision isaf_raid_taliban_hideouts executed"
		}

		days_remove = 15
		days_re_enable = 30

		fixed_random_seed = no

		remove_effect = {
			random_list = {
				90 = {
					news_event = {
						id = TalibanInsurgency.17
						hours = 1
					}
					modifier = {
						factor = 1.2
						AFG = {
							has_stability > 0.5
						}
					}
					modifier = {
						factor = 1.5
						AFG = {
							has_stability > 0.6
						}
					}
				}
				10 = {
					news_event = {
						id = TalibanInsurgency.18
						hours = 1
					}
					modifier = {
						factor = 1.25
						AFG = {
							has_idea = widespread_corruption
						}
					}
					modifier = {
						factor = 1.5
						AFG = {
							has_idea = systematic_corruption
						}
					}
					modifier = {
						factor = 1.75
						AFG = {
							has_idea = unrestrained_corruption
						}
					}
					modifier = {
						factor = 2.25
						AFG = {
							has_idea = rampant_corruption
						}
					}
					modifier = {
						factor = 2.5
						AFG = {
							has_idea = crippling_corruption
						}
					}
					modifier = {
						factor = 3.0
						AFG = {
							has_idea = paralyzing_corruption
						}
					}
				}
			}
			custom_effect_tooltip = isaf_raid_taliban_success_tt
		}

		ai_will_do = {
			factor = 1

			modifier = {
				factor = 10
				AFG = {
					check_variable = {
						var = taliban_strength
						value = 45
						compare = greater_than
					}
				}
			}
		}
	}

	isaf_launch_drone_strike = {
		allowed = { NOT = { original_tag = AFG } }
		icon = GFX_decision_uav

		custom_cost_trigger = { command_power > 40 }
		custom_cost_text = command_power_more_than_40

		highlight_states = {
			highlight_provinces = {
				10737
				12831
				12769
				5064
				4893
				6186
			}
		}


		visible = {
			has_country_flag = afg_support_counter_insurgency
			AFG = {
				check_variable = {
					var = taliban_strength
					value = 0
					compare = greater_than
				}
			}
		}

		available = {
			has_military_access_to = AFG
			has_equipment = {
				small_plane_suicide_airframe > 9
			}
			custom_trigger_tooltip = {
				tooltip = AFG_more_than_40_percent_taliban_strength
				AFG = {
					check_variable = {
						var = taliban_strength
						value = 40
						compare = greater_than
					}
				}
			}
		}

		complete_effect = {
			add_command_power = -40
			log = "[GetDateText]: [This.GetName]: decision isaf_launch_drone_strike executed"
		}

		days_remove = 10
		days_re_enable = 60

		fixed_random_seed = no

		remove_effect = {
			random_list = {
				65 = {
					news_event = {
						id = TalibanInsurgency.21
						hours = 1
					}
					modifier = {
						factor = 1.2
						has_equipment = {
							Air_UAV2 > 0
						}
					}
					modifier = {
						factor = 1.5
						has_equipment = {
							Air_UAV3 > 0
						}
					}
					modifier = {
						factor = 2.5
						has_equipment = {
							Air_UAV4 > 0
						}
					}
				}
				35 = {
					news_event = {
						id = TalibanInsurgency.22
						hours = 1
					}
				}
			}
			custom_effect_tooltip = isaf_drone_strike_taliban_success_tt
		}

		ai_will_do = {
			factor = 1

			modifier = {
				factor = 10
				AFG = {
					check_variable = {
						var = taliban_strength
						value = 75
						compare = greater_than
					}
				}
			}
		}
	}

	isaf_bomb_taliban_hotspots = {
		allowed = { NOT = { original_tag = AFG } }
		icon = GFX_decision_jet_bomber

		custom_cost_trigger = {
			command_power > 40
		}

		custom_cost_text = command_power_more_than_40

		visible = {
			has_country_flag = afg_support_counter_insurgency
			AFG = {
				check_variable = {
					var = taliban_strength
					value = 0
					compare = greater_than
				}
			}
		}

		available = {
			has_military_access_to = AFG
			has_deployed_air_force_size = {
				size > 4
				type = strategic_bomber
			}
			custom_trigger_tooltip = {
				tooltip = AFG_more_than_60_percent_taliban_strength
				AFG = {
					check_variable = {
						var = taliban_strength
						value = 60
						compare = greater_than
					}
				}
			}
		}

		complete_effect = {
			add_command_power = -40
			log = "[GetDateText]: [This.GetName]: decision isaf_bomb_taliban_hotspots executed"
		}

		days_remove = 5
		days_re_enable = 90

		fixed_random_seed = no

		remove_effect = {
			random_list = {
				65 = {
					news_event = {
						id = TalibanInsurgency.23
						hours = 1
					}
					modifier = {
						factor = 1.1
						has_equipment = {
							strategic_bomber2 > 0
						}
					}
					modifier = {
						factor = 1.2
						has_equipment = {
							strategic_bomber3 > 0
						}
					}
					modifier = {
						factor = 1.5
						has_equipment = {
							strategic_bomber4 > 0
						}
					}
					modifier = {
						factor = 2.5
						has_equipment = {
							strategic_bomber5 > 0
						}
					}
					modifier = {
						factor = 4.0
						has_equipment = {
							strategic_bomber6 > 0
						}
					}
				}
				35 = {
					news_event = {
						id = TalibanInsurgency.24
						hours = 1
					}
				}
			}
			custom_effect_tooltip = isaf_bomb_taliban_success_tt
		}

		ai_will_do = {
			factor = 1

			modifier = {
				factor = 10
				AFG = {
					check_variable = {
						var = taliban_strength
						value = 75
						compare = greater_than
					}
				}
			}
		}
	}

	isaf_provide_technical_equipment = {
		allowed = { NOT = { original_tag = AFG } }
		icon = GFX_decision_generic_research_new

		custom_cost_trigger = {
			check_variable = { treasury > 0.5 }
		}

		custom_cost_text = cost_0_5

		visible = {
			has_country_flag = afg_support_counter_insurgency
			AFG = {
				check_variable = {
					var = taliban_strength
					value = 0
					compare = greater_than
				}
			}
			amount_research_slots > 4
		}

		complete_effect = {
			log = "[GetDateText]: [This.GetName]: decision isaf_provide_technical_equipment executed"
			set_temp_variable = { treasury_change = -0.5 }
			modify_treasury_effect = yes
			AFG = {
				news_event = {
					id = TalibanInsurgency.25
					hours = 1
				}
			}
		}

		days_remove = 30
		days_re_enable = 90

		remove_effect = {
			AFG = {
				set_temp_variable = { percent_change = 2 }
				set_temp_variable = { tag_index = FROM.id }
				set_temp_variable = { influence_target = AFG.id }
				change_influence_percentage = yes
				add_stability = 0.01
				set_temp_variable = { taliban_strength_decrease = 5 }
				decrease_taliban_strength = yes
				add_to_variable = { var = taliban_attack_strength value = -0.05 }
				clamp_variable = { var = taliban_attack_strength min = 0 max = 1 }
			}
		}

		ai_will_do = {
			factor = 1

			modifier = {
				factor = 10
				AFG = {
					check_variable = {
						var = taliban_strength
						value = 45
						compare = greater_than
					}
				}
			}
		}
	}

	isaf_withdraw_from_afghanistan = {
		allowed = { NOT = { original_tag = AFG original_tag = USA } }
		icon = GFX_decision_jet_transport
		cost = 50

		visible = {
			has_military_access_to = AFG
			has_country_flag = afg_support_counter_insurgency
			AFG = {
				has_country_flag = afg_active_taliban_insurgency
			}
		}

		available = {
			has_military_access_to = AFG
		}

		complete_effect = {
			log = "[GetDateText]: [This.GetName]: decision isaf_withdraw_from_afghanistan executed"
			set_temp_variable = { arg_popularity = 0.02 }
			add_ruling_outlook_popularity = yes
			AFG = {
				news_event = {
					id = TalibanInsurgency.16
					hours = 1
				}
			}
		}

		days_remove = 30
		fire_only_once = yes

		remove_effect = {
			if = {
				limit = {
					has_military_access_to = AFG
				}
				diplomatic_relation = {
					country = AFG
					relation = military_access
					active = no
				}
			}
			clr_country_flag = afg_support_counter_insurgency
		}

		ai_will_do = {
			factor = 0

			modifier = {
				add = 1
				date > 2015.1.1
			}

			modifier = {
				add = 20
				has_government = nationalist
			}

			modifier = {
				add = 10
				has_government = neutrality
			}

			modifier = {
				add = 100
				has_stability < 0.4
			}
		}
	}

}

# New Taliban Insurgency System
AFG_tal_insurgency_new = {
	# TALIBAN SPECIFIC DECISIONS


	# Border Wars (Shared)








	# Taliban Take-Over Decisions

}
